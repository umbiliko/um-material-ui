import * as React from 'react';
/*
import {
    FormControl,
    FormControlLabel,
    FormGroup,
    FormHelperText,
    FormLabel,
    TextField,
    Select
} from '@material-ui/core';
*/
import { Visual } from 'ntt-view';

export default (props: Visual) => {
    return (
        <form />
    );
};
